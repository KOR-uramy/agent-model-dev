# 「성장·동종 비교」절 병합용 부록 — 마케팅 축 (Agent job7, parallel 1777812867-0adde5c8)

> **병렬 모드 제약**: `RALPH_TASK.md`·`.ralph/progress.md`는 직접 수정하지 않았음. 아래 블록을 `RALPH_TASK.md`의 **`### 성장·동종 비교`** 절 **「마케팅」항목(약 116행)** 아래에 **하위 `- [ ]`** 로 붙이거나, 동일 절 맨 아래에 이어 붙인다.  
> **progress.md 한 줄**(오케스트레이터 복사용): `동종 비교(2026-05-03 parallel 1777812867 job7 마케팅): Intercom·PostHog·Linear — 가치제안·CTA·문서·/register 정합 — 세부는 .ralph/parallel/1777812867-0adde5c8/RALPH_TASK-growth-section-append.md`

## 비교 SaaS 1~3 (이름 · URL · 선정 이유)

| # | 이름 | URL | 선정 이유 |
|---|------|-----|-----------|
| 1 | **Intercom** | https://www.intercom.com | 히어로 **가치 제안·신뢰 블록·1차 CTA** 리듬이 랜딩 표준으로 잘 알려져 있다. |
| 2 | **PostHog** | https://posthog.com | 개발자 제품 **PLG**에서 문서·오픈코어·셀프서브 **전환 경로**가 명확하다. |
| 3 | **Linear** | https://linear.app | B2B SaaS **브랜드 톤·간결한 카피·제품 스크린** 일관성이 벤치마크로 자주 쓰인다. |

## 축별 갭 (마케팅 — bullet)

- **가치 제안**: 제품 기능 나열 대비 **비기술 구매자 한 문장**이 히어로에 고정돼 있지 않을 수 있다(Intercom 대비).
- **전환(CTA)**: `/`, `/login`, `/dashboard`에서 **1차 행동**(대시보드 vs 로그인 vs 문서) 우선순위가 화면마다 달라 PostHog식 PLG 흐름보다 분산될 수 있다.
- **사회적 증거**: 고객 로고·리뷰가 없을 때 **팩트 기반 신뢰**(공개 계약 요약, self-test, 규약 문서 링크)를 몇 개까지 노출할지 **개수·위치**가 정해져 있지 않을 수 있다.
- **문서 링크**: `docs/opengraze-llms-guide.md`는 저장소에만 있어 **배포 URL**만 가진 방문자에게는 상대 경로 안내가 Linear 수준의 “한 클릭 문서”보다 거칠 수 있다.
- **정합성**: `RALPH_TASK.md`·앱 README는 **`/register`** 를 전제로 하나, UI 라우트가 없으면 **신뢰·전환**에 역효과가 난다.

## `RALPH_TASK.md` 「성장·동종 비교」— **마케팅** 항목 아래에 붙일 `- [ ]` (측정·우선순위)

아래는 **구현 완료 시 `[x]`**로 두고, 전부 완료되면 메타 규약대로 동종 비교 라운드를 다시 채운다.

- [ ] **`/register` 정합** — 공개 **`/register`** 페이지(또는 README에서 경로 삭제) 중 하나로 정리하고, 랜딩·로그인 푸터에서 **동일한 회원가입 CTA 라벨**을 노출한다(현재 README와 UI 불일치 시 우선).
- [ ] **히어로 3요소 표** — `/` 에서 **제목·부제(가치 한 줄)·1차 CTA 라벨**을 추출해 Intercom(또는 PostHog) 랜딩 동일 3요소와 **3행 표**로 비교하고, 비기술 구매자용 **부제 대안 1문장**을 `docs/` 또는 병렬 md에 남긴다.
- [ ] **신뢰·문서 노출 개수** — `/`·`/login`·루트 `README.md` 첫 화면(폴드)에서 **외부/내부 문서 링크 또는 명시적 경로 문자열**이 몇 개인지 **정수 N**으로 세고, PostHog “Docs 첫 클릭” 대비 **N 목표(±1)** 한 줄을 적는다.
- [ ] **팩트 기반 사회적 증거** — 고객 로고 없이 쓸 수 있는 증거(예: `platform:self-test`, `/llms.txt`, `detail.role` 규약)를 랜딩에 **불릿 M개**로 고정하고 M을 기록한다; Linear 스타일 **한 줄 인용문**(가상 금지, 실제 커밋/이슈 링크만) 추가 여부를 예/아니오로 결정한다.
- [ ] **배포 호스트 문서** — 프로덕션 베이스 URL에서 **`/llms.txt` 200**과 “장문 가이드”로 연결할 **절대 URL**(또는 “저장소만”) 중 무엇을 마케팅 기본으로 할지 정하고, 랜딩 카피에서 **상대 경로만 쓰는 문장**을 0개로 줄인다(불가 시 예외 한 줄 기록).
- [ ] **대시보드 빈 상태 CTA** — 워크스페이스 0개일 때 **다음 한 단계**(키 발급 vs self-test)를 카피로 고정하고, README **핵심 플로** 번호 목록과 **단계 수 일치** 여부를 예/아니오로 적는다.

---

메타 규약: 위 중 **1개 이상**을 끝낸 뒤, 오케스트레이터가 `RALPH_TASK.md` 마케팅 축과 병합한다.
