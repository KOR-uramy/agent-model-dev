# ── OpenGraze 수집 연동 (어떤 앱·CI에서도 같은 이름 권장) ──
# 대시보드 → 워크스페이스 → 수집용 API 키 에서 발급한 전체 문자열 og_live_...
# 문서: docs/opengraze-llms-guide.md · 런타임 짧은 인덱스: http://localhost:3000/llms.txt (dev 기준)
OPENGRAZE_PLATFORM_URL="http://localhost:3000"
OPENGRAZE_PLATFORM_API_KEY=""
# 선택: Task API 예제·스크립트에서 slug 고정 시
# OPENGRAZE_WORKSPACE_SLUG="open-graze-self"
# 플랫폼 자기 연동 스모크 — `npm run platform:self-test` (위 키 필수)
