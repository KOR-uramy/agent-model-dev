# Secrets / local config
.env
.env.*
!.env.example

# Ralph (Cursor) — ralph-config may contain API keys
.cursor/ralph-config.json
.ralph-worktrees/

# OS / editor
.DS_Store

# npm workspaces (루트에서 hoisting)
node_modules/

# Ralph 런타임 이벤트 로그 (로컬 전용)
.ralph/events.jsonl
