# Secrets / local config
.env
.env.*
!.env.example

# Ralph (Cursor) — ralph-config may contain API keys
.cursor/ralph-config.json
.ralph-worktrees/

# OS / editor
.DS_Store

# npm workspaces (루트에서 hoisting)
node_modules/

# Ralph 런타임 상태 전부(진행·캐시·로그·병렬 산출물 — 레포에 올리지 않음). 가이드·Signs 는 docs/ralph-guardrails.md
.ralph/
